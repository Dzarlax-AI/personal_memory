<!-- Personal Memory integration bundle {{.BundleVersion}}; contract {{.ContractVersion}} -->
<!-- PERSONAL_MEMORY_CANONICAL_POLICY_BASE64 {{.CanonicalPolicyBase64}} -->
## Managed Personal Memory policy

Capability state: memory={{.Memory}}, documents={{.Documents}}, todoist={{.Todoist}}.

The canonical policy below is managed content. User customizations belong in the separate override path declared by the manifest.

{{.CanonicalPolicyMarkdown}}
